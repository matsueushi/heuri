type Contest @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
  description: String!
}

type Submission @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  contestId: ID!
  func: String!
  description: String!
}
